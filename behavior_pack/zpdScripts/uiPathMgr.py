class PanelPath:

    class AttributePanel:
        attributePanel = "/attributePanel"
        bg_img = attributePanel + "/bg_img"
        testData = attributePanel + "/testData"

    class SkillPanel:

        skill = "/skill"
        skillGrid = skill + "/skillGrid"
    
class PresetPath:

    class attribute:
        
        progress_bar = "/progress_bar"
        text = progress_bar + "/text"

    class skill:

        mainBtn = "/mainBtn"
        skillProgressBar = "/skillProgressBar"